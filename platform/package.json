{
  "name": "@foc/platform",
  "version": "0.1.0",
  "private": true,
  "description": "Shared service runtime: config validation, logging, correlation, health, error envelope",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nestjs/common": "^12.0.3",
    "@nestjs/config": "^12.0.0",
    "@nestjs/core": "^12.0.3",
    "amqplib": "^2.0.1",
    "dotenv": "^17.0.0",
    "pino": "^10.3.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.8",
    "@types/express": "^5.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  },
  "type": "module"
}
