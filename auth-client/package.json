{
  "name": "@foc/auth-client",
  "version": "0.1.0",
  "private": true,
  "description": "Shared auth middleware: verifies FoC access tokens and resolves the caller's live identity via the User Service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@foc/platform": "*",
    "@nestjs/common": "^12.0.3",
    "@nestjs/core": "^12.0.3",
    "jose": "^6.2.12",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@nestjs/platform-express": "^12.0.3",
    "@nestjs/testing": "^12.0.3",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.2.2",
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  },
  "type": "module"
}
